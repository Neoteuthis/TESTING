using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SPAWNER : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        //   Rigidbody floorInstance;
        //  floorInstance = Instantiate(floorbox,pos, rot) as Rigidbody2D;
       // Instantiate();
    }

   // private Rigidbody2D Instantiate(object floorbox, int v1, int v2)
  //  {
      //  throw new NotImplementedException();
  //  }
}
