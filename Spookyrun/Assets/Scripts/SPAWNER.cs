using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SPAWNER : MonoBehaviour {
    public GameObject platform;
    public Transform spawnpoint;

    public float distance;
    static public bool canspawn;
    // Use this for initialization
    void Start() {

    }

    // Update is called once per frame
   // void Update() {
        //   Rigidbody floorInstance;
        //  floorInstance = Instantiate(floorbox,pos, rot) as Rigidbody2D;
        // Instantiate();
        //check for spawn necessity
        // if (canspawn == true) {
        //  }
      //  public Vector3 spawnervec = new Vector3(0, 50, 0);//transform.position.x = 0;
        // Instantiate(platform, spawnervec);
      //  public static extern GameObject Instantiate(platform, spawnervec);
   // }

   // private Rigidbody2D Instantiate(object floorbox, int v1, int v2)
  //  {
      //  throw new NotImplementedException();
  //  }
}
